from django.urls import path

from restaurant.views import index, DishTypeListView, DishTypeCreateView

urlpatterns = [
    path("", index, name="index"),
    path(
        "dish_types/",
        DishTypeListView.as_view(),
        name="dish-type-list",
    ),
    path(
        "dish_types/create/",
        DishTypeCreateView.as_view(),
        name="dish-type-create",
    ),
]

app_name = "restaurant"
